global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yaml

scrape_configs:
  - job_name: dgx-control
    scheme: http
    metrics_path: /metrics
    bearer_token_file: /run/secrets/metrics-token
    static_configs:
      - targets: [control-api:8000]
