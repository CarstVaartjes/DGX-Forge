diff --git a/ds4_server.c b/ds4_server.c
index 0f4834a..c2c7734 100644
--- a/ds4_server.c
+++ b/ds4_server.c
@@ -933,0 +934,8 @@
+static bool server_model_id_is_valid(const char *id) {
+    if (!id || !id[0]) return false;
+    for (const unsigned char *p = (const unsigned char *)id; *p; p++) {
+        if (iscntrl(*p)) return false;
+    }
+    return true;
+}
+
@@ -934,0 +943,2 @@
+    const char *configured = getenv("DS4_SERVED_MODEL_NAME");
+    if (server_model_id_is_valid(configured)) return configured;
@@ -939,4 +949,2 @@
-static bool server_model_alias_known(const char *id) {
-    return id &&
-           (!strcmp(id, "deepseek-v4-flash") ||
-            !strcmp(id, "deepseek-v4-pro"));
+static bool server_model_alias_known(ds4_engine *engine, const char *id) {
+    return id && !strcmp(id, server_model_id_from_engine(engine));
@@ -13684 +13692 @@
-        server_model_alias_known(hr.path + model_path_prefix_len))
+        server_model_alias_known(s->engine, hr.path + model_path_prefix_len))
