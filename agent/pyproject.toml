[project]
name = "dgx-agent"
version = "0.1.0"
description = "DGX Forge outbound agent runtime scaffold"
requires-python = ">=3.12"
dependencies = ["dgx-agent-protocol==1.0.0"]

[tool.uv.sources]
dgx-agent-protocol = { path = "../agent_protocol" }
